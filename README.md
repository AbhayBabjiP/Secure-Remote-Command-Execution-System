# Secure-Remote-Command-Execution-System
A secure, multi-threaded client-server system built with Python that supports authenticated remote command execution and file transfer over TLS using a structured JSON protocol, with logging and performance evaluation.
